```bash
# docker install
sudo apt update && sudo apt upgrade -y
sudo apt install docker.io -y
```

```yml
# start
chmod +x start-mtproxy.sh
./start-mtproxy.sh
```

На телефоне нажми на сгенерированную ссылку `tg://...`
